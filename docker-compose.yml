services:
  app:
    build: .
    container_name: snippetbox
    ports:
      - "${APP_PORT}:4000"
    depends_on:
      - db

  db:
    image: mysql:8.1
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data: